{% extends "base.j2" %}

{% block content %}

  <h1>Your Submissions</h1>

  {% for item in submissions %}
    <div class="card info">
      <p>{{ item.statement_text }}</p>
      {% if item.vote != 0 %}
        <p>
          your vote:
          {% if item.vote == 1 %}
            Yes
          {% else if item.vote == -1 %}
            No {% endif %}
        </p>
      {% endif %}
      <p>yes: {{ item.yes_count }}, no: {{ item.no_count }}</p>
      <p>{{ self.human_relative_time(item.author_timestamp) }}</p>
      <input type="hidden" value="{{ item.statement_id }}" />
    </div>
  {% endfor %}

{% endblock %}
