{% extends "base.j2" %}

{% block content %}

  <h1>Recent Votes</h1>

  {% for item in history %}
    <div class="card info">
      <p>{{ item.statement_text }}</p>

      <p>
        Your Vote:
        {% if item.vote == 1 %}
          Yes
        {% else if item.vote == -1 %}
          No
        {% endif %}
      </p>
      <p>{{ self.human_relative_time(item.vote_timestamp) }}</p>

      <input type="hidden" value="{{ item.statement_id }}" />

    </div>
  {% endfor %}

{% endblock %}
